import styled from '@emotion/styled';
import { colors } from '../../../../colors';

export const ItemBox = styled.div`
  display: flex;
  align-items: center;
  h1 {
    margin: 0.75em;
    font-size: 18px;
    color: ${colors.blue};
  }
`;
